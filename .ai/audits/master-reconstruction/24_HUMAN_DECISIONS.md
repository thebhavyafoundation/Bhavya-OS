# 24 — Human Decisions Required

Genuinely blocked on human institutional authority (everything else continues autonomously):

1. **Source B provenance.** Was a new lesson collection supplied out-of-band? Where is the canonical copy? Who authored it, under what license, for what ages? Until answered: keep separation model (§11), land nothing.
2. **Constitution home + numbering.** `docs/constitution/` (9, live) vs `packages/constitution/` (SDK claim) vs promised docs 10–15. Which is canonical? Rename/remap or write the missing docs?
3. **Binding metrics.** 10,000ha/1M-resources (MISSION.md) vs 100ha/500-KPs (00 Art.4) vs 8ha/10k-students/50-communities (01 Art.10) vs 100-enrollments (PRD). Declare ONE binding set; rest become historical/aspirational with labels.
4. **Content SSOT.** `content-core` doctrine vs app SQLite reality. Authorize extraction (with provenance) or amend constitution?
5. **Route prefixes.** `/missions/forest` + `/academy` vs `/forest` + `/knowledge/academy`. Declare winner; other becomes redirect.
6. **L1 domain list.** `02/08` set vs `03` set. Declare canonical set.
7. **Pillar wording.** "Three Pillars" header + 3-item tagline vs 4 missions. Fix header + tagline or redefine Community's status.
8. **KP-001 disposition.** Complete the 6 artifacts, delist "Gold Standard", or mark demo? It currently presents as real.
9. **Unsourced public numbers.** Source/date/verify or remove: 14,200 ha, 100%-contributions, 500+/230+/331 stats, 10k-student claims.
10. **Deploy precedence + branch.** Vercel vs Cloudflare; `master` vs `main` in `deploy.yml`. Declare production path + fix trigger.
11. **GitHub OS autonomy bounds.** What may agents mutate autonomously? What needs human approval? Codify allowlist + self-modification guards.
12. **Student-memory privacy.** Retention, ownership, who may read learning data (incl. minors in Himachal cohort). Required before mentor/analytics work.
13. **Missing docs 10–15 subjects** (volunteers, donations, finance, conflict-of-interest, child protection live partly in `_archive/constitution-2026-09-05/` 15 files — adopt/adapt or supersede?).

Non-blocking (documented, continue): token rebuild direction (forest wins per ai-institute CONTEXT.md), duplicate-package retirements, `github-os.zip` deletion, BIN rename, video-engine import fix, stale 8-level curriculum mark, analytics ownership.
