# 09–10 — AI Institute + Existing Curriculum (Source A)

## Institute surface (apps/ai-institute)

- 127 `page.tsx` routes. Layers: public (`/`, `/about`, `/forest`, `/heritage`, `/knowledge`, `/curriculum`, `/courses`, `/library`, `/research`, `/community`, `/volunteer`, `/donate`, `/programs`, `/schools`, `/learning-paths`, …), authenticated `/app/*` (learn, knowledge, missions, research, projects, forest, credentials, contributions, profile), institutional `/os/*` (admin, docs, ioc, github, knowledge, student, volunteer, donor, trustee, forest, social, memory, runtime, observability, search, videos, bin, api-explorer), authoring `/studio/*` (courses, lessons, knowledge + CRUD APIs), API (`academy/published`, `auth/*`, `forest/*`, `heritage/*`, `student/progress`, `studio/*`, `transparency`, `volunteers`, `mentors`).
- Student journey vs spec (`LEARNING_JOURNEY.md` 13 stages; `student-journey.md` 6 phases/19 screens): IMPLEMENTED shells — landing, paths, curriculum, onboarding, dashboard, lesson reader, portfolio page, studio. PARTIAL — labs (6 in `lab-exercises.ts` vs 7 spec'd; no Socratic/hints), quizzes (40+ static Qs, no adaptivity/spaced repetition), projects (no GitHub milestones). STUB — mentor (no conversational mentor), certification (`/app/credentials`, no verifiable flow), capstone (research JSON is evidence store, no flow), personalized roadmap, career/placement (no route).
- Auth: `lib/api-auth.ts` (bcrypt-12, session-token cookie, `stripSensitive`), `lib/require-role.ts`, `lib/route-policy.ts` (SSOT), `lib/roles.ts`, `middleware.ts` (protected `/app /dashboard /os /studio /onboarding /api/studio`; public `/os /os/knowledge /os/search`; CSRF + hard-403 fallback + noindex + CSP/HSTS). Tests: `auth.test.ts`, `authorization-matrix.test.ts`. Note: unknown paths fall back to session-only (`canAccess` true) — tighten during hardening.

## Existing curriculum — Source A (fully inventoried, DO NOT merge)

- Code truth: `src/data/academy-lessons.ts` (479KB; IDs `found-*`, `py-*`, `dl-*`, `llm-*` + `-ex-N`; ~231 lesson+exercise IDs), `academy-courses.ts` (course→module→lesson map), `lesson-content.ts` (103KB bodies), `assessments.ts` (40+ Qs), `learning-paths.ts`, `lab-exercises.ts` (6 labs), `curriculum-levels.ts` (L0–L12), `knowledge-packages.ts` (KP-001 only), `projects.ts`, `progress.ts`, `knowledge-graph.ts` (55KB).
- Spec truth: `docs/master-curriculum/` (12 files; 13 levels L0 Digital Foundations → L12 Institution Building; 74 modules à 6 per level except L0=2; MODULE_CATALOG 1659 lines; KP backlog mostly unbuilt) + `docs/ai-institute/` (37 top-level + curriculum/ + review/ + runtime/ + slices/ + student-validation/; includes older 8-level + 5-track model in `CURRICULUM_ARCHITECTURE.md` — STALE, unreconciled).
- Coverage reality: implemented lessons cover ~L1 subset only (`found` 9, `py` 9, `dl` 9, `llm` partial). L0, L2–L12 lessons live in spec/backlog only. `knowledge-packages/level-1/KP-001-*/README.md` references 6 artifacts (`Lesson/Assessment/Teacher-Guide/Workbook/Visual-Spec/Website`) — NONE on disk. 1 of ~300+ planned KPs exists, as a stub presenting "Gold Standard".
- Curriculum hierarchy (code): Program > Level (0–12) > Module (`found-mod-1..3`…) > Lesson (`found-1-1`) > Exercise (`-ex-1`) + Track/Path/Lab/Assessment/Project/KnowledgePackage.
