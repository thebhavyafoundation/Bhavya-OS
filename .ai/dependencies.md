---
id: RUNTIME-009
owner: Engineering
version: 0.5
status: active
depends:
  - RUNTIME-004
related:
  - RUNTIME-010
  - STD-003
---

# Dependency Map

Machine-readable source of truth: `.ai/dependencies.yaml` (generated by the
runtime compiler) and `registry/`. This page summarises the workspace
app → package edges for humans.

## Apps → Packages

| App                                | `@bhavya/*` dependencies                                                                                                                                                                                                                           |
| ---------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `apps/admin`                       | `auth`, `mission-runtime`, `platform-ui`, `sdk`                                                                                                                                                                                                    |
| `apps/ai-institute`                | `content-core`, `database`, `impact-runtime`, `platform-ui`, `project-runtime`                                                                                                                                                                     |
| `apps/bhavya-intelligence-network` | `auth`, `intelligence`, `capability-registry`, `crawlers`, `mcp-manager`, `plugin-manager`, `browser-automation`, `github-intelligence`, `technology-radar`, `analyzers`, `knowledge-extraction`, `events`, `workflows`, `platform`, `platform-ui` |
| `apps/design-system`               | `platform-ui`                                                                                                                                                                                                                                      |
| `apps/docs`                        | `mission-runtime`, `sdk`                                                                                                                                                                                                                           |
| `apps/github-os`                   | `auth`, `database`, `events`, `platform`, `platform-ui`, `shared`, `types`                                                                                                                                                                         |
| `apps/ioc`                         | `auth`, `database`, `platform-ui`                                                                                                                                                                                                                  |
| `apps/social-os`                   | `auth`, `database`, `events`, `platform`, `platform-ui`, `types`                                                                                                                                                                                   |

## Package Dependencies

The complete package → package graph lives in `.ai/dependencies.yaml`
(generated). `registry/packages.json` lists the 62 workspace packages.

## Dev Dependency Chain

| Config               | Used By                                   |
| -------------------- | ----------------------------------------- |
| `@bhavya/typescript` | `apps/admin`, `apps/docs` (`workspace:*`) |
| `@bhavya/eslint`     | `apps/admin`, `apps/docs` (`workspace:*`) |

The remaining apps use the root toolchain (`turbo`, `typescript`, `eslint`).

## External Dependencies

| Package       | Version | Used By    |
| ------------- | ------- | ---------- |
| `next`        | 15.5.25 | all apps   |
| `react`       | 19.2.4  | all apps   |
| `react-dom`   | 19.2.4  | all apps   |
| `turbo`       | ^2.5.6  | root (dev) |
| `typescript`  | ^5.9.2  | root (dev) |
| `tailwindcss` | ^4.1.13 | root (dev) |
| `eslint`      | ^9.36.0 | root (dev) |
| `prettier`    | ^3.6.2  | root (dev) |
| `husky`       | ^9.1.7  | root (dev) |

## No Dependency Cycles

The dependency graph is acyclic: Apps → Packages → Utilities. No circular dependencies exist or are permitted.
