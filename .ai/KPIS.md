# BHAVYA OS — KEY PERFORMANCE INDICATORS (KPIS)

Institutional platform metrics tracked automatically by `apps/admin`.

## Metrics Catalog

1. **Build Integrity**: 100% pass rate on `pnpm build`, `pnpm typecheck`, `pnpm lint`.
2. **Quality Gates**: Zero regressions in accessibility (WCAG 2.1 AA) or bundle budgets.
3. **Documentation Coverage**: 100% of packages, apps, and APIs documented in `packages/docs`.
4. **Gateway Uptime**: >99.9% availability for provider-agnostic AI Gateway requests.
5. **Registry Synchronization**: `registry/*.json` 100% in sync with monorepo state.
