{
  "name": "@bhavya/kernel",
  "version": "3.1.0",
  "description": "Bhavya Kernel - The core runtime for Bhavya OS",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "echo \"Kernel tests are custom harnesses — no vitest test blocks\"",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "@bhavya/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
